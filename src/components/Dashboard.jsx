import { Link } from "react-router-dom";

const Dashboard = () => {
    return (
        <div>
            <Link to='/products'>go to products</Link>
        </div>
    )
};

export default Dashboard;
